{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
{{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}


<h3>Rechnungsanschrift</h3>


{{ form_widget(form) }}


<button class="btn btn-success float-right" type="submit">Weiter</button>

{{ form_end(form) }}
</div>
{% endblock %}